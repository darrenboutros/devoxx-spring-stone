spring:
  cloud:
    consul:
      config:
        watch:
          enabled: true
    
      discovery:
        instance-id: ${spring.application.name}:${random.value}
        tags:        - urlprefix-/v1/stone/{stonePosition}
        - urlprefix-/v1/stones
          
  application:
    name: stone-service